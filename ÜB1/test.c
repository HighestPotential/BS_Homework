#include <stdio.h>

int main(void) {

struct test
{
    int x;
    int y;
    /* data */
};

struct test Point2;
Point2.x=11;
Point2.y=33;

return 0;
}


// 1. Anwendungen

// Aufgabe:

// Anwendungen (Application Software) erfüllen konkrete Aufgaben für den Nutzer.

// Sie kommunizieren über das Betriebssystem mit der Hardware und stellen die gewünschte Funktion bereit.

// Beispiel:

// VS Code → ermöglicht das Programmieren, Ausführen von Code, Debuggen.

// 2. Nutzer

// Aufgabe:

// Der Nutzer ist der Mensch, der die Anwendungen bedient, um eine Aufgabe zu erledigen.

// Der Nutzer interagiert direkt mit den Anwendungen, nicht direkt mit dem Betriebssystem oder der Hardware.

// Beispiel:

// Ein Softwareentwickler, der in VS Code programmiert.

// 3. Betriebssystem

// Aufgabe:

// Das Betriebssystem (Operating System, OS) ist die Vermittlungsschicht zwischen Anwendungen und Hardware.

// Es verwaltet Ressourcen wie CPU, Speicher, Peripherie und ermöglicht, dass mehrere Programme gleichzeitig ausgeführt werden (Prozessverwaltung, Parallelisierung).

// Beispiele:

// Windows 11, Linux, macOS

// Parallelisierung von Prozessen → mehrere Programme laufen gleichzeitig ohne dass sie sich gegenseitig stören.

// 4. Hardware

// Aufgabe:

// Hardware ist die physische Grundlage des Systems: alles, was man anfassen kann.

// Sie führt die Befehle der Anwendungen über das Betriebssystem aus.

// Beispiel:

// GPU NVIDIA 5090 → verarbeitet Grafikdaten, unterstützt Berechnungen für Programme.

Aufgabe 2:
a) man <Command>
